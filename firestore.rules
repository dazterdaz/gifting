rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS COMPLETAMENTE ABIERTAS PARA DESARROLLO
    // IMPORTANTE: Cambiar estas reglas en producción
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}