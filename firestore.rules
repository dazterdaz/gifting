rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para usuarios autenticados
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // Reglas para superadmin
    function isSuperAdmin() {
      return isAuthenticated() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'superadmin';
    }
    
    // Reglas para admin o superadmin
    function isAdmin() {
      return isAuthenticated() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['admin', 'superadmin'];
    }

    // Colección de usuarios - acceso más permisivo temporalmente
    match /users/{userId} {
      // Permitir todas las operaciones temporalmente para resolver problemas de inicialización
      allow read, write: if true;
    }

    // Colección de giftcards - acceso más permisivo temporalmente
    match /giftcards/{giftcardId} {
      // Permitir todas las operaciones temporalmente
      allow read, write: if true;
    }

    // Colección de actividades - acceso más permisivo temporalmente
    match /activities/{activityId} {
      // Permitir todas las operaciones temporalmente
      allow read, write: if true;
    }

    // Colección de mensajes de contacto - acceso más permisivo temporalmente
    match /contactMessages/{messageId} {
      // Permitir todas las operaciones temporalmente
      allow read, write: if true;
    }

    // Configuración del sitio - acceso más permisivo temporalmente
    match /settings/{settingId} {
      // Permitir todas las operaciones temporalmente
      allow read, write: if true;
    }

    // Términos y condiciones - acceso más permisivo temporalmente
    match /terms/{termId} {
      // Permitir todas las operaciones temporalmente
      allow read, write: if true;
    }

    // Documento de inicialización - acceso completo
    match /{collection}/_init {
      allow read, write: if true;
    }

    // Documento de prueba de conexión - acceso completo
    match /test/connection {
      allow read, write: if true;
    }
  }
}